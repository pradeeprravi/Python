import random as r
import string

length=6
otp=''
characters=string.ascii_letters+string.digits
#print(characters)

# for i in range(length):
#   print(r.choice(characters))

for i in range(length):
  otp=otp+r.choice(characters)
print(otp)
